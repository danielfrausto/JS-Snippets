   Array.from(document.getElementsByClassName("Class")).forEach(function (element) {
                ['Event-0','Event-1','Event-2'].forEach(function (event) { element.addEventListener(event, function(){
                  //
                  //do stuff here
                  //
                }), false })
            })
